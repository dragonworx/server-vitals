{
  "name": "vitals",
  "version": "1.0.0",
  "description": "Lightweight server vitals endpoint — a host-monitoring agent (not a container). See README.",
  "private": true,
  "comment": "vitals is dependency-free Python; this file exists ONLY so `bun run <name>` can drive the service. Each script wraps the matching Makefile target, which stays the source of truth. No deps, so no `bun install` is needed.",
  "scripts": {
    "start": "make start",
    "stop": "make stop",
    "restart": "make restart",
    "deploy": "make deploy",
    "status": "make status",
    "logs": "make logs",
    "dev": "make run",
    "check": "make check"
  }
}
