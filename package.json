{
  "name": "server-vitals",
  "version": "2.0.1",
  "description": "Single-file, zero-dependency host-monitoring agent (Python stdlib only). This package.json exists only to expose convenience scripts.",
  "private": true,
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/dragonworx/server-vitals"
  },
  "scripts": {
    "prep": "./doc/png-to-webp.sh",
    "publish:patch": "npm version patch --no-git-tag-version && V=$(node -p \"require('./package.json').version\") && sed -i \"s/^VERSION = .*/VERSION = \\\"$V\\\"/\" server-vitals.py && git add -A && git commit -m \"v$V\" && git tag \"v$V\" && git push && git push --tags",
    "publish:minor": "npm version minor --no-git-tag-version && V=$(node -p \"require('./package.json').version\") && sed -i \"s/^VERSION = .*/VERSION = \\\"$V\\\"/\" server-vitals.py && git add -A && git commit -m \"v$V\" && git tag \"v$V\" && git push && git push --tags",
    "publish:major": "npm version major --no-git-tag-version && V=$(node -p \"require('./package.json').version\") && sed -i \"s/^VERSION = .*/VERSION = \\\"$V\\\"/\" server-vitals.py && git add -A && git commit -m \"v$V\" && git tag \"v$V\" && git push && git push --tags"
  }
}
